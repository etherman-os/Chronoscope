# WARNING: This compose file is for local development ONLY.
# Internal services (postgres, redis, minio) bind to host ports for convenience.
# Volume paths are resolved relative to this file's directory (docker/).
services:
  postgres:
    image: postgres:16-alpine
    container_name: chronoscope-postgres
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_USER: chronoscope
      POSTGRES_PASSWORD: chronoscope
      POSTGRES_DB: chronoscope
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../migrations:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chronoscope -d chronoscope"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: chronoscope-redis
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:RELEASE.2024-10-13T13-34-11Z
    container_name: chronoscope-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: chronoscope
      MINIO_ROOT_PASSWORD: chronoscope123
    ports:
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  minio_data:
