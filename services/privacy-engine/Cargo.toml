[package]
name = "chronoscope-privacy-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
image = "0.24"
thiserror = "1.0"

[lib]
name = "chronoscope_privacy"
crate-type = ["cdylib", "staticlib", "rlib"]
